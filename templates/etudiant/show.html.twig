{% extends 'base.html.twig' %}

{% block title %}Etudiant{% endblock %}

{% block body %}
    <h1>Etudiant</h1>
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Etudiant</h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ etudiant.id }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ etudiant.email }}</td>
            </tr>
{#            <tr>#}
{#                <th>Roles</th>#}
{#                <td>{{ dump(etudiant.roles) }}</td>#}
{#            </tr>#}
            <tr>
                <th>Nom</th>
                <td>{{ etudiant.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ etudiant.prenom }}</td>
            </tr>
            <tr>
                <th>Date_naissance</th>
                <td>{{ etudiant.dateNaissance ? etudiant.dateNaissance|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ etudiant.sexe }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ etudiant.ville }}</td>
            </tr>
        </tbody>
    </table>
        </div>
    </div>
</div>
    <div class="row">
        <div class="col-9">
            <a class="btn btn-secondary btn-icon-split" href="{{ path('etudiant_index') }}">
        <span class="icon text-white-50">
          <i class="fas fa-arrow-left"></i>
        </span>
                <span class="text">Retour à la liste</span>
            </a>
            <a class="btn btn-warning btn-icon-split" href="{{ path('etudiant_edit', {'id': etudiant.id}) }}">
        <span class="icon text-white-50">
          <i class="fas fa-exclamation-triangle"></i>
        </span>
                <span class="text">Modifier</span>
            </a>
        </div>
        <div class="col-3">
            {{ include('etudiant/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
