{% extends 'base.html.twig' %}

{% block title %}Stage - show{% endblock %}

{% block body %}
<h1>Stage</h1>
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Stage</h6>
    </div>
    <div class="card-body">
        <div class="table-responsive">

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stage.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ stage.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ stage.description }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ stage.type }}</td>
            </tr>
            <tr>
                <th>Duree</th>
                <td>{{ stage.duree }}</td>
            </tr>
            <tr>
                <th>Date_debut</th>
                <td>{{ stage.dateDebut ? stage.dateDebut|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Disponibilitee</th>
                <td>{{ stage.disponibilitee ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
        </div>
    </div>
</div>

    <div class="row">
        <div class="col-9">
            <a class="btn btn-secondary btn-icon-split" href="{{ path('stage_index') }}">
        <span class="icon text-white-50">
          <i class="fas fa-arrow-left"></i>
        </span>
                <span class="text">Retour à la liste</span>
            </a>
            <a class="btn btn-warning btn-icon-split" href="{{ path('stage_edit', {'id': stage.id}) }}">
        <span class="icon text-white-50">
          <i class="fas fa-exclamation-triangle"></i>
        </span>
                <span class="text">Modifier</span>
            </a>
        </div>
        <div class="col-3">
            {{ include('stage/_delete_form.html.twig') }}
        </div>
    </div>



{% endblock %}

